import React from "react";
import {
    // Box,
    Grid
} from "@chakra-ui/react";

const Main = (props) => {
    return (
        <Grid templateColumns="auto 1fr" gap='6'>

        </Grid>
    );
};

export default Main;